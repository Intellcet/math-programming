import React from 'react';

import './App.pcss';
import MatrixBlock from '../MatrixBlock';

const App = (): React.ReactElement => (
  <React.Fragment>
    <MatrixBlock />
  </React.Fragment>
);

export default App;
