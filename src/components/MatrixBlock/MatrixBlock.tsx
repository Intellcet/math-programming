import React from 'react';

import styles from './MatrixBlock.module.pcss';

const MatrixBlock = (): React.ReactElement => {
  return (
    <div className={styles.matrixContainer}>Here will be matrix soon!!!!</div>
  );
};

export default MatrixBlock;
